import { FormContact } from '../../../features/Contact';

import './Contact.scss';

const Contact = () => {
  return (
    <div className="contact">
      <FormContact />
    </div>
  );
};

export default Contact;
