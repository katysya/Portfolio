import { SectionContact } from '../../../features/Contact';

import './Contact.scss';

const Contact = () => {
  return (
    <div className="contact">
      <SectionContact />
    </div>
  );
};

export default Contact;
